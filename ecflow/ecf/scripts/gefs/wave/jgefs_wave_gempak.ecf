#BSUB -J %E%gefs_wave_gempak_%CYC%
#BSUB -o /com/output/%ENVIR%/today/gefs_wave_gempak_%CYC%.o%J
#BSUB -cwd /tmp
#BSUB -n 1
#BSUB -R span[ptile=1]
#BSUB -R "affinity[core(1):distribute=pack]"
#BSUB -R "rusage[mem=4608]"
#BSUB -q %QUEUE%_shared
#BSUB -W 03:30
#BSUB -L /bin/sh
#BSUB -P %PROJ%-%PROJENVIR%

module purge

%include <head.h>
%include <envir-p35.h>
set -x

export model=gefs
%include <model_ver.h>

. /usrx/local/prod/lmod/lmod/init/ksh
module load EnvVars/$EnvVars_ver
module load ips/$ips_ver
module load impi/$impi_ver
module load prod_util/$prod_util_ver
module load prod_envir/$prod_envir_ver
module load grib_util/$grib_util_ver
module load gempak/$gempak_ver
module load lsf/$lsf_ver
module load CFP/$CFP_ver
export USE_CFP=YES

module list

# EXPORT list here
ulimit -s unlimited
ulimit -a

export RUNMEM=ge%MEMBER%

export total_tasks=1
export taskspernode=1

# CALL executable job script here
${HOMEgefs}/jobs/JGEFS_WAVE_GEMPAK

%include <tail.h>
%manual
# include manual page below
%end
